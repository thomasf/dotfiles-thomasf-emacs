;; just empty, because somehow ../makeup.el gets compiled into php-mode.el
;; requireing a package called makeup as well, I do not have time to
;; investigate atm. leave this stub instead.
(provide 'makeup)

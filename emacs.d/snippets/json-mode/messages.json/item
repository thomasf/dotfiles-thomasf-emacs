# -*- mode: snippet -*-
# name: item (mesages.json)
# key: item
# condition: (and buffer-file-name (string= (file-name-nondirectory buffer-file-name) "messages.json"))
# --
,
"$1": {
  "message": "$0"
}
